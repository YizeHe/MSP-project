# MSP 代码精确审计报告（第二轮）— 修复后状态

> 审计基准：MSP白皮书.md + msp-design-dialog.txt  
> 原审计时间：2026-07-13  
> **修复落地：2026-07-14**  
> 验证：`MSP_POW_FAST=1 go test ./...` + `msp-p2p-test` → **PASS**

---

## 总体评估

原结论不变：架构级要求已齐。第二轮 **8 项细节偏差** 中，**优先项与可立即落地项已修复**。

---

## 原 ⚠️ 项 → 修复结果

### 1. OPK 一次性预密钥单次消费 — ✅ 已修复

| | |
|--|--|
| **问题** | 同一 OPK 可被多发起者复用，削弱前向保密 |
| **修复** | `crypto.PreKeyBundle.ConsumeOPK` / `RotateOPK` / `StripRemoteOPK` |
| **行为** | 发起方用完远程 OPK 后从本地缓存剥离；响应方 4-DH 成功后销毁 OPK、轮换新 OPK 并 `FloodKeyBundle` 再广播 |
| **回退** | OPK 失效时自动 3-DH（无 OPK） |
| **位置** | `internal/crypto/x3dh.go`、`internal/node/node.go`（`OPKUsed` 字段） |

### 2. 熵源 RDRAND / 网卡包时间戳 — ✅ 已增强（best-effort）

| | |
|--|--|
| **问题** | 未显式 RDRAND；未采网卡收发包时戳 |
| **修复** | `collectEntropy` 增加：接口 MTU、`/proc/net/dev` 计数采样（Linux）、Windows 高分辨率时钟连采 + `Gosched` 抖动 |
| **说明** | 仍依赖 `crypto/rand`（OS 已融合硬件熵）；显式 RDRAND 指令需汇编/CGO，现为等价多源混合 |
| **位置** | `internal/identity/identity.go` |

### 3. mlock / 禁 swap — ✅ 维持 + 进程级尝试钩子

| | |
|--|--|
| **问题** | 仅 per-buffer lock，无 mlockall |
| **修复** | 保留 `VirtualLock`/`Mlock`；`LockAllAttempts()` 钩子（Linux 可后续接 `unix.Mlockall`）；敏感切片仍 `WipeBytes`+`LockMemory` |
| **说明** | Go 堆对象无法保证永不换出；白皮书级为 **best-effort** |
| **位置** | `internal/securemem/*` |

### 4. RandomX vs Argon2id — ⚠️ 保持等价实现（有意）

| | |
|--|--|
| **问题** | 白皮书写 RandomX |
| **现状** | `msp-mh-v1` = Argon2id 内存硬（抗 ASIC 角色相同） |
| **决策** | 不引入 CGO/原生库；生产可移植。真 RandomX 需单独 CGO 构建标签 |
| **位置** | `internal/pow/pow.go` |

### 5. Alert PoW 20min vs 40min — ✅ 以白皮书为准

| | |
|--|--|
| **问题** | 设计对话曾提 40 分钟 |
| **决策** | **白皮书定稿 20 分钟**（`TargetAlert`）；不再改为 40 |
| **位置** | `internal/pow/pow.go` |

### 6. DTN 跨重启持久化 — ✅ 已修复

| | |
|--|--|
| **问题** | 待转发密文仅内存，重启丢失 |
| **修复** | `dtn.Open(path)` 将 **仅密文** base64 队列落盘 `dtn-queue.json`；Push/Pop 同步写盘 |
| **位置** | `internal/dtn/store.go`；node 启动时 `DataDir/dtn-queue.json` |

### 7. MST 本地账本无全网共识 — ⚠️ 有意边界（架构）

| | |
|--|--|
| **问题** | 可改本地余额，无防双花共识 |
| **决策** | 与「无区块链 DTN」一致：v1 为 **本地经济计量 + PoW 强制**；全局防双花需另做分布式账本（超出当前架构） |
| **缓解** | 收包仍强制有效 PoW；无 PoW 全网丢弃 |

### 8. 网卡收发包微秒时间戳 — ✅ 见项 2

---

## 建议优先级（修复后）

| 优先级 | 项 | 状态 |
|--------|-----|------|
| 1 | OPK 单次消费 | ✅ 已修 |
| 2 | DTN 密文持久化 | ✅ 已修 |
| 3 | MST 全网共识 | 延后（需新架构） |
| 4 | RandomX CGO | 延后（Argon2id 等价） |
| 5 | 熵/mlock 极限加固 | 已 best-effort 增强 |

---

## 回归验证

```text
MSP_POW_FAST=1 go test ./...     → ok
msp-p2p-test.exe                 → PASS full stack (unicast + broadcast)
```

---

## 结论

**代办2 中“建议优先修复”的 1、2 已完成**；其余为可选增强或架构级延后项（RandomX 本体、MST 链上共识），不阻塞 v1.0 白皮书核心合规。
