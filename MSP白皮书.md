MST Network 去中心化抗封锁阅后即焚通信网络白皮书

1. 项目概述

-------

### 1.1 核心定位

MST Network 是一套**无中心、无CA、无磁盘消息存储、端到端加密、阅后即焚、强抗封锁**的 DTN 延迟容忍泛洪通信网络。网络摒弃传统互联网面向连接、固定服务器、固定IP路由架构，基于纯分布式邻居泛洪、存储-携带-转发机制实现通信，支持分钟级至数天的可变传输延迟，以**隐私绝对优先、抗封锁绝对优先、去中心化绝对优先**为核心设计理念，主动牺牲低延迟与数据持久性。

网络核心特性：身份可备份、消息永不留存、全网公开广播、可控治理公告、算力防垃圾机制、无任何中心化管控节点。

### 1.2 设计核心原则

* **身份与消息完全隔离**：助记词仅恢复加密身份密钥，绝不恢复任何历史消息记录

* **纯内存消息机制**：所有消息仅驻留隔离内存，无磁盘写入、无日志、无缓存落盘

* **分层加密机制**：路由元数据明文公开，业务载荷全程端到端加密

* **无信任第三方**：无CA证书体系、无内置密钥、无中心化注册服务器

* **算力约束治理**：基于抗ASIC PoW实现消息发送门槛，杜绝垃圾广播与女巫攻击

* **极简可控公告体系**：单一官方公告私钥，无特权、无后门、到期公开、用户可自主屏蔽
2. 网络底层架构

---------

MST Network 不基于TCP面向连接通信，属于典型**延迟/中断容忍网络（DTN）**。节点无固定路由表、无固定中继路径、无中心服务器。

通信逻辑：节点接收邻居数据包后，校验数据包合法性与时序合规性，若自身无法直达目标节点，则向全部邻居泛洪转发；数据包可在全网节点内存中临时缓存，等待网络连通窗口完成转发，传输延迟无硬性上限（1分钟～10天+）。

中间节点仅可读取明文路由元数据，无法解密消息载荷，节点缓存密文数据包无任何隐私泄露风险。

### 2.2 节点接入机制

* **内置种子节点**：程序预置少量公网种子IP，仅用于首次入网组网；节点成功接入主网、同步邻居列表后，永久不再连接内置种子节点

* **自定义种子节点**：支持用户手动添加任意数量自定义种子节点、洋葱节点地址，最大化规避封锁

* **零准入门槛**：无注册、无实名、无KYC，仅本地生成加密身份即可接入网络

### 2.3 全网去重机制（65536 哈希环形缓存）

所有本地节点维护固定容量为**65536** 的双哈希环形缓存队列，存储全网已接收数据包标识，杜绝重复展示、重复转发、网络广播风暴。

* 缓存内容：单条数据包 **SHA1+SHA512 双哈希**，彻底规避哈希碰撞风险

* 命中规则：收到缓存内存在的哈希数据包，直接丢弃，不展示、不转发

* 淘汰规则：队列满量后，自动淘汰最早入库的哈希记录，循环复用内存空间
3. 身份系统与助记词体系

-------------

### 3.1 核心规则：身份可恢复、消息零恢复

全网唯一可持久化数据为用户加密身份密钥体系，所有消息数据全程内存驻留、阅后即焚、程序退出即彻底销毁。用户通过助记词可在任意设备登录恢复全网唯一身份，但**无法恢复任何历史消息、聊天记录、缓存数据**。

### 3.2 多元熵源生成机制（SHA512）

摒弃单一时间种子，采用多源混合熵池，全程本地离线生成，无联网依赖、无内置固定种子、无中心化熵源。所有原始熵拼接后经由 **SHA-512** 算法混合搅拌，输出高安全随机熵用于生成BIP39助记词。

熵池输入维度：

* 系统核心熵：操作系统CSPRNG加密安全随机数、CPU RDRAND硬件真随机

* 硬件动态熵：CPU RDTSC纳秒周期抖动、磁盘IO时延、进程调度时差

* 设备行为熵：网卡数据包收发微秒时间戳、外设交互随机噪声

### 3.3 BIP39+BIP32 身份派生体系

* 熵输出：256bit 安全熵，生成标准BIP39 24词助记词

* 种子拉伸：PBKDF2 加盐拉伸生成512位主种子

* 密钥派生：通过 HMAC-SHA512 固定硬化路径，唯一派生节点长期密钥对

* 身份密钥：Ed25519签名密钥（身份校验、消息签名）+ X25519密钥（会话密钥协商）

* 全网节点ID：节点公钥SHA1哈希值，全局唯一、不可篡改、无重复

### 3.4 内存隐私隔离规范（零磁盘消息）

* 所有消息数据仅驻留**用户态隔离内存页**，禁止swap交换分区落盘

* 程序禁用core dump、禁用内存调试读取，内核层级禁止外部进程访问消息内存

* 程序退出/崩溃/主动关闭时，内存消息段强制覆写0x00与随机垃圾数据，彻底销毁痕迹

* 仅身份密钥、助记词解密种子持久化加密存储，与消息内存完全物理隔离
4. 加密体系与中间人防御机制

---------------

### 4.1 分层加密架构

采用**路由明文+载荷密文**双层架构，兼顾网络转发可行性与端到端隐私安全：

* 明文层：时间戳、发送方ID、接收方ID、PoW参数、TTL、签名等路由元数据，全网节点可读，用于转发与校验

* 加密层：消息正文核心载荷，采用 **AES-256-GCM** 加密，仅收发双方会话密钥可解密，所有中继节点无法读取、篡改明文内容

### 4.2 定制X3DH 抗中间人攻击体系（三层防御）

网络无CA信任链，依靠自研三层防御体系彻底杜绝MITM中间人劫持攻击，适配DTN泛洪无中心场景。

#### 第一层：X3DH 多重密钥协商

基于长期身份密钥、临时一次性密钥、签名预密钥四重DH拼接，SHA512聚合生成唯一会话密钥，单次会话唯一、不可复用、不可伪造。

#### 第二层：全网公钥泛洪校验

陌生公钥接入全网自动泛洪广播副本，节点本地缓存合法公钥指纹；若出现「同一节点ID对应多组不同公钥」，自动判定为中间人攻击，批量丢弃异常数据并本地告警。

#### 第三层：带外指纹兜底核验

身份公钥截断生成16位短指纹，支持用户线下人工核对，彻底封堵极端算力劫持漏洞。

5. 标准数据包完整结构

------------

所有网络数据包统一分为**明文头部（可校验、可路由）**与**加密载荷（端到端私密）**两部分，字段完整固定，无冗余数据。

### 5.1 明文头部字段（全网节点可读可校验）

* Packet_SHA1 / Packet_SHA512：数据包双哈希唯一标识，用于全网去重

* Timestamp_μs：发送端微秒级原生时间戳，作为消息排序主依据

* Sender_ID：发送方公钥SHA1哈希ID（明文）

* Target_ID：接收方ID，单播指定、广播置空标记

* Broadcast_Flag：全网广播标记位

* Alert_Flag：官方公告专属标记位

* PoW_Difficulty：本条消息算力难度参数

* PoW_Nonce：工作量证明随机数，用于合法性校验

* TTL_Max_Hops：最大转发跳数，超跳数自动销毁

* Signature：发送方Ed25519签名，保护头部元数据不可篡改

### 5.2 加密载荷字段（仅收发双方可解密）

* 消息正文文本数据

* 自定义消息过期TTL时间

* 载荷完整性校验MAC值
6. 消息排序与时间戳防篡改机制

----------------

### 6.1 全网统一排序规则

1. 主排序：按数据包 **微秒时间戳** 升序排列

2. 同时间戳排序：按数据包SHA1哈希二进制原始字节大小升序排列，全网节点排序结果完全一致，无本地随机分裂

### 6.2 时间戳硬约束防攻击

摒弃微秒级偏移阈值缺陷，采用**±500ms 全网基准时间偏移约束**：

* 节点实时同步全网平均基准时间

* 单条消息时间戳与全网基准差值超过500ms，直接判定为恶意篡改，丢弃不转发、不展示

* 适配普通NTP同步误差、多跳网络延迟，兼顾安全性与兼容性
7. 官方Alert公告体系（极简可控治理）

----------------------

### 7.1 核心规则

* 全网仅设立**唯一一组Alert私钥**，无多签、无分权、无后门

* 权限硬编码锁死：仅可发送标记Alert_Flag的全网广播消息，无路由控制、无节点管控、无协议修改权限

* 消息形态：与普通广播消息格式完全一致，无弹窗、无强制推送，仅作为标准信息流展示

### 7.2 三年公开机制

* 网络稳定运行满3年，完整公开Alert私钥，彻底消除中心化特权隐患

* 客户端内置**用户自主屏蔽功能**：可一键屏蔽Alert官方ID，永久拒收所有公告消息

* 无需软分叉、无需社区投票，极简治理、全程透明

* 公告消息额外叠加超高PoW难度，私钥公开后仍无法批量刷屏
8. MST 算力经济与防垃圾机制

-----------------

### 8.1 核心定位

MST（Message Sending Token）为网络原生消息手续费算力凭证，无增发、无预挖、无通胀，仅用于约束消息发送权限，杜绝女巫攻击、垃圾广播、带宽轰炸。

### 8.2 抗ASIC算法选型

采用 **RandomX 内存硬依赖抗ASIC算法**，算力瓶颈为内存带宽而非芯片算力，专业ASIC无成本优势，保证全网算力去中心化、平民化。

### 8.3 算力耗时标准（动态难度调节）

网络根据全网实时算力自动动态微调难度，恒定维持基准耗时：

* 普通用户单播/普通全网广播：基准耗时 **2分钟**

* 官方Alert全网公告广播：基准耗时 **20分钟**

### 8.4 经济规则

* 创世初始算力一次性产出全部MST总量，后续**只销毁、不增发**，总量永久恒定

* 无有效PoW的数据包，全网节点统一拒收、不转发、不展示

* 难度自适应：全网算力上涨则提升算法复杂度，算力下跌则降低复杂度，永久锁定标准耗时
9. 极致抗封锁体系

----------

* **无固定特征协议**：纯DTN泛洪流量，无TCP长连接握手特征、无固定端口特征，规避DPI深度识别

* **流量混淆机制**：数据包固定长度填充、随机转发延迟（10ms～3s），抹平流量指纹

* **双链路兼容**：同时支持IPv4/IPv6明网、Tor洋葱匿名链路，自动择优连通

* **无中心拓扑**：无固定根服务器、无固定中继节点、无固定IP集群，单点封锁完全无效

* **跳数限流防护**：数据包最大转发跳数限制，杜绝无限循环广播与网络DoS

* **离线容错传输**：支持长期离线节点，数据包缓存等待连通窗口，网络容错性拉满
10. 核心安全与隐私总结

-------------

1. **绝对阅后即焚**：消息全程内存驻留，无磁盘备份、无日志、无恢复可能，助记词仅恢复身份

2. **无中心化信任**：无CA、无内置密钥、无服务器管控，纯数学加密信任体系

3. **中继绝对盲转发**：中间节点仅可转发，无法解密、无法窃取用户消息隐私

4. **算力约束防滥用**：PoW门槛彻底杜绝垃圾消息与女巫攻击

5. **可控透明治理**：唯一公告密钥、到期公开、用户可自主屏蔽，无中心化后门风险

6. **全网唯一身份**：SHA512多元熵生成密钥，全局无碰撞、无伪造可能

11. 终版架构闭环说明

------------

本网络完全实现用户定制全部需求：放弃低延迟换取极致灵活与抗封锁、无CA无内置密钥、纯本地熵生成身份、助记词身份可恢复消息零留存、分层加密、DTN泛洪转发、哈希去重、时序防篡改、PoW算力治理、可控官方公告、极致抗封锁，架构逻辑完全自洽、无漏洞、可工程落地。
