# MSP 公链设计 v2：链管经济，消息走 DTN — 实现状态

> 设计原则：公链仅 MST 账本与 burn 凭证；消息 100% DTN  
> **代码状态：已按此改造（v1.2.0-economy-chain）**  
> 更新：2026-07-14

---

## 实现对照

| 设计点 | 状态 | 位置 |
|--------|------|------|
| 链上零消息内容 | ✅ | 已删除 `direct_msg` / `broadcast_msg` 交易类型 |
| burn 仅 MsgType + RefHash | ✅ | `chain.BurnData` + `BurnForMessage` |
| transfer / genesis_claim / register | ✅ | `chain/types.go` + `engine.go` |
| 发消息先查余额再 DTN 再 burn | ✅ | `app.Service.Send/Broadcast` + `submitBurn` |
| 接收纯 DTN | ✅ | `onChainBlock` 不再注入 inbox |
| 矿工收手续费 | ✅ | `state.ApplyBlock` pendingMinerFees |
| 创世 21e6 / 认领 1000 / 2 年窗口 | ✅ | `GenesisSupply` / `ClaimWindow` |
| 块 PoW Argon2id | ✅ | 复用 `pow` 包 |
| P2P 广播块/交易 | ✅ | `TypeChainBlock` / `TypeChainTx` |

## 已知限制

- 多节点最长链分叉策略仍较简（接受更高 height）
- header-first 同步未单独实现（整块广播）
- GUI 无独立「区块浏览器」页（`chain` CLI 可查）

## 验证

```bash
set MSP_POW_FAST=1
set MSP_CHAIN_FAST=1
go test ./internal/chain -count=1
go build -o msp.exe ./cmd/msp
msp -c chain-claim
msp -c chain   # mode: economy-only
```
