# MSP 公链 + DTN 双层消息架构 — 实现状态

> 设计见原方案；**已落地 v1.1.0-chain**（2026-07-14）  
> 验证：`go test ./internal/chain` + 单节点 `chain-claim` 出块

---

## 已实现模块

| 模块 | 路径 | 说明 |
|------|------|------|
| 区块/交易 | `internal/chain/types.go` | Header + 5 类 Tx |
| Merkle | `internal/chain/merkle.go` | 交易 Merkle 根 |
| 状态账本 | `internal/chain/state.go` | 账户余额模型 + StateRoot |
| 内存池 | `internal/chain/mempool.go` | 待打包交易 |
| 创世 | `internal/chain/genesis.go` | height=0 + init_alert |
| 共识/挖矿 | `internal/chain/consensus.go` + `engine.go` | Argon2id 块 PoW，最长链追加 |
| 持久化 | `internal/chain/store.go` | `dataDir/chain/blocks.json` |
| P2P 广播 | `p2p.TypeChainBlock/Tx` | 块与交易在 mesh 上扩散 |
| 双层路由 | `app.Service.Send` | 在线 → `direct_msg`；离线 → DTN + `dtn_relay` 凭证 |

## 交易类型与费用（代办3）

| Type | Fee |
|------|-----|
| genesis_claim | 0 |
| transfer | 1 MST |
| direct_msg | 2 MST |
| dtn_relay | 10 MST |
| broadcast_msg | 5 MST |
| alert broadcast | 20 MST |

## CLI

```text
msp -c chain | chain-status
msp -c chain-claim          # 链上创世认领 1000 MST 并出块
msp -c chain-mine [force]
msp -c chain-transfer <to> <amount>
msp -c send-chain <id> <text>   # 强制链上 direct_msg
msp -c send <id> <text>         # 双层自动路由
```

环境：`MSP_CHAIN_FAST=1` 或 `MSP_POW_FAST=1` → 出块间隔 15s、低 PoW。

## 与设计差异 / 后续

| 项 | 状态 |
|----|------|
| header-first 全历史同步 | 简化为接收完整块并校验；多节点可广播块 |
| 复杂分叉选择 | 当前仅接受更高 height 的合法块 |
| 区块浏览器 GUI | 可用 `chain-status`；GUI 页可后续加 |
| 10 分钟出块 | 默认 10min；实验室 15s |

DTN 层保持原样（高成本、阅后即焚）；公链为在线低成本通道。
