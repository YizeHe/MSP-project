# 代办7：DTN 层常量同步 + 自动 claim 逻辑清理

> 代办6 只更新了链层（chain/），DTN 层（mst/）和应用层（app/）仍是旧值
> 生成时间：2026-07-13

---

## 一、待修改清单

### 1. `internal/mst/ledger.go` — 常量同步

```go
// 旧值（第 16、19 行）
GenesisSupply uint64 = 21_000_000
GenesisGrant  uint64 = 1000

// 新值
GenesisSupply uint64 = 4_226_880
GenesisGrant  uint64 = 128
```

仅改这两行，其余代码（ClaimGenesis、Burn、Snapshot）逻辑不变，自动使用新值。

### 2. `internal/app/service.go` — 自动 claim 逻辑

**删除第 244-252 行**（StartMesh 中的自动认领）：

```go
// 删除这段
if ledger != nil {
    if _, _, claimed := ledger.Snapshot(); !claimed {
        if err := n.ClaimGenesisMST(); err != nil {
            s.Log("dtn mst genesis: " + err.Error())
        } else {
            s.Log("DTN MST genesis claimed")
        }
    }
}
```

**理由**：自动 claim 意味着每个节点启动就领 MST，不需要用户主动操作。这违背"必须通过挖矿获得"的设计意图。改为用户手动 `msp -c claim-genesis` 或通过挖矿获得。

### 3. `internal/app/cli.go` — 帮助文本

```go
// 旧（第 327 行）
  -c mst | claim-genesis

// 新
  -c mst | claim-genesis          (128 MST, 210 slots, one-time)
```

仅补充说明文字，命令本身保留（手动 claim 仍允许）。

### 4. `internal/app/service.go` — MSTInfo 引用

```go
// 旧（第 684 行）
"genesis_supply": mst.GenesisSupply, "grant": mst.GenesisGrant,

// 无需改动，自动使用 mst 包新常量
```

确认 `mst.GenesisSupply` 和 `mst.GenesisGrant` 更新后此处自动生效。

### 5. `cmd/msp-p2p-test/main.go` — 测试

```go
// 第 69-70 行
must(nA.ClaimGenesisMST())
must(nB.ClaimGenesisMST())
```

**保留**。这是 DTN 层集成测试，需要 MST 余额才能发消息。ClaimGenesisMST() 使用 mst 包的新常量（128 MST），自动生效。

---

## 二、验证步骤

```bash
# 1. 编译通过
go build ./...

# 2. 链层测试
go test ./internal/chain/ -v

# 3. DTN 层测试
go test ./internal/mst/ -v

# 4. 集成测试
go test ./cmd/msp-p2p-test/ -v

# 5. 全量测试
go test ./... -v

# 6. 手动验证
msp -c init --print-mnemonic
msp -c start
msp -c mst              # 确认 grant=128, supply=4226880
msp -c claim-genesis    # 手动认领 128 MST
msp -c mst              # 确认 balance=128
```

---

## 三、工作量

| 任务 | 文件 | 时间 |
|------|------|------|
| 改常量 | `mst/ledger.go` | 2 分钟 |
| 删自动 claim | `app/service.go` | 2 分钟 |
| 更新帮助文本 | `app/cli.go` | 2 分钟 |
| 编译 + 测试 | 全项目 | 10 分钟 |
| **合计** | | **~15 分钟** |
