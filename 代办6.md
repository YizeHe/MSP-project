# MSP 公链设计 v4：降低免费认领额度

> 核心变更：genesis_claim 保留，但降低到 128 MST/节点，仅限 210 台节点
> 生成时间：2026-07-13

---

## 一、参数变更

| 参数 | 旧值 | 新值 |
|------|------|------|
| GenesisGrant（每节点认领量） | 1000 MST | **128 MST** |
| 最大认领节点数 | 21,000 | **210** |
| 认领池总量 | 16,800,000 MST | **26,880 MST** |
| 矿工奖励池 | 4,200,000 MST | **4,200,000 MST**（不变） |
| 全网 MST 总量 | 21,000,000 MST | **4,226,880 MST** |
| ClaimWindow（认领窗口） | 2 年 | **2 年**（不变） |

**经济模型**：
- 认领池仅 26,880 MST（210 × 128），用于早期节点启动
- 绝大部分 MST（420 万）来自挖矿
- 新节点主要靠挖矿获得 MST，认领只是冷启动辅助

---

## 二、需要修改的代码

### 文件 1：`internal/chain/types.go`

```go
// 旧值
GenesisGrant    uint64 = 1000
GenesisSupply   uint64 = 21_000_000
MinerRewardPool uint64 = 4_200_000
ClaimableSupply uint64 = GenesisSupply - MinerRewardPool // 16_800_000

// 新值
GenesisGrant    uint64 = 128
MaxClaimNodes   uint64 = 210
GenesisSupply   uint64 = 4_226_880           // 4,200,000 + 26,880
MinerRewardPool uint64 = 4_200_000           // 不变
ClaimableSupply uint64 = 26_880              // 210 × 128
ClaimWindow     = 2 * 365 * 24 * time.Hour  // 不变
```

### 文件 2：`internal/chain/state.go`

`applyGenesisClaim()` 方法增加节点数上限检查：

```go
func (s *State) applyGenesisClaim(tx *Transaction, blockTime time.Time) error {
    if blockTime.After(NetworkGenesis.Add(ClaimWindow)) {
        return errClaimClosed
    }
    a := s.ensure(tx.Sender)
    if a.Claimed {
        return errAlreadyClaimed
    }
    // 新增：检查认领节点数上限
    claimCount := s.TotalClaimed / GenesisGrant
    if claimCount >= MaxClaimNodes {
        return errSupplyExhausted
    }
    if s.TotalClaimed+GenesisGrant > ClaimableSupply {
        return errSupplyExhausted
    }
    // ... 其余不变
}
```

### 文件 3：`internal/chain/engine_test.go`

更新测试中的余额断言：

```go
// 旧
wantBal := GenesisGrant + BlockReward(1)

// 新（GenesisGrant 从 1000 变为 128）
wantBal := GenesisGrant + BlockReward(1)  // 128 + 50 = 178
```

更新总量断言：

```go
// 旧
if ClaimableSupply != 16_800_000 || MinerRewardPool != 4_200_000 {

// 新
if ClaimableSupply != 26_880 || MinerRewardPool != 4_200_000 {
```

### 其他文件：无结构性修改

以下文件**不需要改动**（自动使用新常量值）：
- `chain/engine.go` — `ClaimGenesis()` 逻辑不变
- `chain/errors.go` — 错误定义不变
- `chain/genesis.go` — 创世区块不变
- `app/service.go` — `ChainClaimGenesis()` 逻辑不变
- `app/cli.go` — `chain-claim` 命令逻辑不变
- `node/node.go` — `ClaimGenesisMST()` 逻辑不变
- `mst/ledger.go` — 独立 DTN 账本，不受链上常量影响
- `alert/alert.go` — 不受影响

---

## 三、210 个认领名额用完后

```
210 个节点认领完毕 → 认领池耗尽 → 新节点只能挖矿

新节点启动流程：
1. msp init                  # 创建身份（免费）
2. msp -c start              # 启动 mesh + 链
3. msp -c chain-claim        # 尝试认领 → 若还有名额则成功
                             # 若 210 名额已满 → 报错 "supply exhausted"
4. msp -c chain-mine force   # 手动出块，靠挖矿获得 MST
5. 等待出块...                # 第一块奖励 50 MST
6. msp -c mst                # 查看余额
7. msp -c send <id> hello    # 余额足够时可发消息
```

---

## 四、防垃圾效果

| 场景 | 效果 |
|------|------|
| 攻击者批量注册 1000 个身份 | 只有前 210 个能领 128 MST，其余 790 个必须挖矿 |
| 攻击者用 210 个身份领完 26880 MST | 最多消耗 26880 MST 后必须挖矿，无法无限刷 |
| 正常用户 | 128 MST 足够发 12 条 DTN 消息或 64 条直传消息，之后靠挖矿补充 |

**128 MST 的设计意图**：
- 足够新节点体验功能（发十几条消息）
- 不足以大规模刷垃圾（单节点 12 条 DTN 就耗尽）
- 主要 MST 来源仍是挖矿（50 MST/块）

---

## 五、开发工作量

| 任务 | 预估时间 |
|------|----------|
| 修改 `chain/types.go` 常量 | 5 分钟 |
| 修改 `chain/state.go` 节点数检查 | 15 分钟 |
| 更新测试断言 | 30 分钟 |
| 验证全链路 | 30 分钟 |
| **合计** | **~1.5 小时** |

---

## 六、与代办5 的差异汇总

| 项 | 代办5 | 代办6 |
|----|-------|-------|
| 每节点认领量 | 1000 MST | **128 MST** |
| 最大认领节点数 | 21,000 | **210** |
| 认领池总量 | 16,800,000 | **26,880** |
| 全网总量 | 21,000,000 | **4,226,880** |
| 矿工池 | 4,200,000 | **4,200,000**（不变） |
| 主要 MST 来源 | 认领（80%） | **挖矿（99.36%）** |
| 防垃圾 | 弱 | **强** |
