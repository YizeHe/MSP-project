# MSP 代办清单 — 全量补齐状态（v1.0.0）

> 对照白皮书 + 原差距清单。更新：2026-07-14  
> 验证：`MSP_POW_FAST=1 go test ./...` + `msp-p2p-test` PASS

## 图例

- ✅ 已实现并接入主路径  
- ⚠️ 已实现，算法/工程等价替代（见备注）  

---

## 一、加密体系

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 1.1 X3DH 四重 DH | ✅ | `internal/crypto/x3dh.go` + `node.sessionKeyFor` / `decrypt` |
| 1.2 全网公钥泛洪 + 冲突检测 | ✅ | `KindKeyFlood` + `trust.Observe` 冲突拒收 |
| 1.3 带外指纹核验 | ✅ | `msp -c verify-fingerprint`；`RequireFingerprint` 可强制 |
| 1.4 显式载荷 MAC | ✅ | `payload_mac` HMAC-SHA512 + GCM |

## 二、PoW / MST

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 2.1 抗 ASIC 内存硬 PoW | ⚠️ | `msp-mh-v1` = **Argon2id**（内存硬）。真 RandomX 需 CGO/原生库；本实现为可移植生产等价物 |
| 2.2 动态难度 | ✅ | `pow.Network` 按耗时贴近 2min/20min（`MSP_POW_FAST=1` 时 ~2s/8s） |
| 2.3 MST 代币 | ✅ | `internal/mst`：总量 21e6、创世领取、发送销毁 |
| 2.4 无 PoW 强制丢弃 | ✅ | 接收路径严格 `pow.Verify` |

## 三、身份

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 3.1 多元熵 | ✅ | CSPRNG+时间+磁盘IO延迟+调度抖动+网卡+循环抖动 → SHA512 |
| 3.2 内存隔离 | ✅ | `securemem` 覆写销毁、`VirtualLock`/`mlock`、禁 core dump 尽力 |
| 3.3 身份加密存储 | ✅ | `MSP_PASSPHRASE` / `--passphrase` AES-GCM 加密助记词 |

## 四、网络

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 4.1 内置种子 | ✅ | `config.BuiltInSeeds`；有邻居后停用内置偏好 |
| 4.2 多种子 | ✅ | `seed list/add/remove/set` |
| 4.3 流量混淆 | ✅ | 定长填充 `PadTo=2048` + 转发延迟 10ms~3s |
| 4.4 IPv6 + Tor | ✅ | `udp4+udp6`；`SOCKS5_PROXY`/`ALL_PROXY` 走洋葱信令 |
| 4.5 离线 store-carry-forward | ✅ | `internal/dtn` 密文队列，邻居上线重发 |

## 五、数据包

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 5.1 独立 MAC 字段 | ✅ | `payload_mac` |
| 5.2 签名范围 | ✅ | 元数据+密文绑定；Hops 可变不进签名 |

## 六、时间排序

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 6.1 ±500ms + 网络时钟 | ✅ | `clocksync` + 默认 `EnforceClock` |
| 6.2 SHA1 原始字节排序 | ✅ | `bytes.Compare` on decoded SHA1 |

## 七、Alert

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 7.1 唯一 Alert 密钥 | ✅ | `internal/alert` 硬编码权威；仅权威可发 alert |
| 7.2 三年公开 | ✅ | `open_deadline` + `should_publish` + 公开 seed 字符串 |
| 7.3 屏蔽 Alert | ✅ | `msp -c block-alert` |

## 八、去重

| 项 | 状态 | 实现位置 |
|----|------|----------|
| 8.1 双哈希 65536 环 | ✅ | `internal/dedup` |

## 九、GUI / CLI

| 项 | 状态 | 实现位置 |
|----|------|----------|
| GUI | ✅ | `msp` 默认启动 Web GUI |
| 完整 CLI | ✅ | `msp -c <全部命令>` 与 GUI 同 `app.Service` |

---

## 环境变量

| 变量 | 作用 |
|------|------|
| `MSP_POW_FAST=1` | 实验室快速 PoW |
| `MSP_PASSPHRASE` | 身份文件加密 |
| `SOCKS5_PROXY` / `ALL_PROXY` | Tor 等 SOCKS 信令 |
| `MSP_ALERT_SEED` | 覆盖 Alert 种子（运维） |
| `MSP_DATA` | 数据目录 |

## 已知工程边界（诚实说明）

1. **RandomX 本体**：未链入 C 版 RandomX；使用 **Argon2id 内存硬 PoW** 承担抗 ASIC 角色。若必须 monero-RandomX，需 CGO 与平台原生库。  
2. **CLI 无守护进程**：每个 `msp -c` 是独立进程；网络命令会按需 `EnsureMesh`。长期在线请用 GUI 或 `msp -c start` 后在同一进程扩展（可用 GUI）。  
3. **MST**：本地账本 + 创世领取 + 发送销毁；非全局链上共识账本（与无区块链的 DTN 设定一致）。
