# MSP 公链设计 v3：修复代办4审计三大缺陷

> 针对代办4审计报告的 3 个编码前必须解决的问题，给出具体方案
> 生成时间：2026-07-13

---

## 修复一：消息合法性验证（burn 确认前如何防垃圾）

### 问题

DTN 消息毫秒级送达，burn 交易 10 分钟后才上链。接收方收到消息时链上无 burn 记录，无法判断消息是否已 burn。

### 方案：BurnTicket 预烧凭证

发送方在发消息**之前**，先在 mempool 中提交 burn 交易，获得一个签名凭证（BurnTicket），附在消息数据包中。接收方在 mempool 中验证凭证有效性。

```
发送流程：
1. 构造 burn 交易 → 提交到 mempool（不上链）
2. 矿工为 burn 交易签发 BurnTicket（签名确认）
3. 将 BurnTicket 附在 DTN 数据包头部
4. 通过 DTN mesh 发送消息

接收流程：
1. 收到 DTN 数据包
2. 提取 BurnTicket
3. 验证 BurnTicket 签名（矿工公钥）
4. 验证 RefHash 匹配消息内容
5. 验证 burn 金额 >= 消息类型要求
6. 全部通过 → 接受消息
```

### BurnTicket 结构

```go
type BurnTicket struct {
    BurnTxHash [32]byte  // burn 交易哈希
    MsgType    string    // "direct" | "dtn" | "broadcast"
    RefHash    [32]byte  // 消息内容 SHA-256
    Amount     uint64    // burn 金额
    Nonce      uint64    // 防重放
    Timestamp  int64     // 微秒时间戳
    MinerSig   [64]byte  // 矿工 Ed25519 签名
    MinerID    string    // 矿工 NodeID
    BlockHint  uint64    // 预期打包区块高度（可选）
}
```

### 矿工签发流程

```go
// 矿工在出块前为 mempool 中的 burn 交易签发 BurnTicket
func (m *Miner) IssueBurnTicket(burnTx Transaction) BurnTicket {
    ticket := BurnTicket{
        BurnTxHash: burnTx.Hash(),
        MsgType:    burnTx.Data.MsgType,
        RefHash:    burnTx.Data.RefHash,
        Amount:     burnTx.Data.Amount + burnTx.Fee,
        Nonce:      rand.Uint64(),
        Timestamp:  time.Now().UnixMicro(),
        MinerID:    m.NodeID,
    }
    ticket.MinerSig = ed25519.Sign(m.PrivKey, ticket.SignBytes())
    return ticket
}
```

### 接收方验证逻辑

```go
func (n *Node) validateBurnTicket(pkt Packet, ticket BurnTicket) error {
    // 1. 矿工签名有效
    minerPub := n.lookupMinerPub(ticket.MinerID)
    if !ed25519.Verify(minerPub, ticket.SignBytes(), ticket.MinerSig) {
        return errors.New("invalid miner signature")
    }

    // 2. RefHash 匹配消息内容
    msgHash := sha256.Sum256(pkt.PayloadCipher)
    if msgHash != ticket.RefHash {
        return errors.New("ref hash mismatch")
    }

    // 3. burn 金额足够
    required := burnCost(ticket.MsgType)
    if ticket.Amount < required {
        return errors.New("insufficient burn")
    }

    // 4. 票据未过期（24 小时内有效）
    if time.Now().UnixMicro()-ticket.Timestamp > 24*3600*1e6 {
        return errors.New("burn ticket expired")
    }

    // 5. BurnTxHash 在 mempool 或已上链中存在
    if !n.Mempool.Has(ticket.BurnTxHash) && !n.Chain.HasTx(ticket.BurnTxHash) {
        return errors.New("burn tx not found")
    }

    return nil
}
```

### 安全性分析

| 攻击场景 | 防御 |
|----------|------|
| 伪造 BurnTicket | 矿工签名不可伪造 |
| 复用 BurnTicket | RefHash 绑定消息内容，一票一用 |
| Burn 后退款 | burn 交易不可逆，mempool 中已锁定 |
| 恶意矿工签假票 | 接收方可验证矿工签名，作假矿工声誉受损 |
| 票据过期重放 | 24 小时有效期 + Nonce |

### 数据包新增字段

在现有 `protocol.Packet` 中增加：

```go
type Packet struct {
    // ... 现有字段 ...

    // Burn 凭证（链上经济层）
    BurnTicket  []byte `json:"burn_ticket,omitempty"` // 序列化的 BurnTicket
    BurnTicketSig []byte `json:"burn_ticket_sig,omitempty"` // 矿工签名（冗余，方便快速验证）
}
```

---

## 修复二：发送方隐私（burn 交易不暴露 NodeID）

### 问题

burn 交易的 `Sender` 字段是发送方 NodeID，链上永久记录"谁 burn 了多少"，通信活跃度可被分析。

### 方案：一次性 Burn 地址（Burner Keypair）

发送方每次 burn 生成一个临时密钥对（burner），burn 交易用 burner 地址提交，不暴露真实 NodeID。链上仅记录 burner 地址，无法关联到真实身份。

```
发送方真实身份: NodeID_A (Ed25519 公钥 SHA1)
burner 临时身份: NodeID_BURN_xxx (一次性 Ed25519)

链上记录:
  burn 交易 → Sender: NodeID_BURN_xxx, Amount: 10 MST
  ❌ 无法关联到 NodeID_A
```

### 实现

```go
func (n *Node) sendAnonymousBurn(msgType string, refHash [32]byte, amount uint64) (*BurnTicket, error) {
    // 1. 生成一次性 burner 密钥对
    burnerPub, burnerPriv, _ := ed25519.GenerateKey(rand.Reader)
    burnerID := protocol.NodeIDFromPub(burnerPub)

    // 2. 从主账户转账 MST 到 burner 地址（链上 transfer 交易）
    //    这一步暴露了 "主账户 → burner" 的关联
    //    但 burner 地址是一次性的，用完即弃
    transferTx := Transaction{
        Type:   "transfer",
        Sender: n.ID.NodeID,
        Data:   TransferData{To: burnerID, Amount: amount + 1}, // +1 for fee
    }
    transferTx.Sign(n.ID.EdPrivate())
    n.Mempool.Submit(transferTx)

    // 3. 用 burner 地址提交 burn 交易（不暴露真实身份）
    burnTx := Transaction{
        Type:   "burn",
        Sender: burnerID, // 一次性地址
        Fee:    1,
        Data:   BurnData{MsgType: msgType, RefHash: refHash, Timestamp: n.Clock.Now().UnixMicro()},
    }
    burnTx.Sign(burnerPriv)
    n.Mempool.Submit(burnTx)

    // 4. 从矿工获取 BurnTicket
    ticket := n.requestBurnTicket(burnTx)
    return ticket, nil
}
```

### 隐私层级

| 方案 | 链上暴露 | 隐私级别 |
|------|----------|----------|
| 直接用 NodeID burn | 发送方 NodeID + burn 金额 + 时间 | ❌ 低（代办4 原设计） |
| 一次性 burner 地址 | burner 地址 + burn 金额 + 时间；主账户→burner 转账可关联 | 🟡 中 |
| 多跳 burner 链 | 主账户 → burner1 → burner2 → burn；多次转账混淆关联 | 🟢 高 |
| 环签名 burn | 发送方隐藏在 N 个公钥中 | 🟢🟢 很高 |

**建议 v1 用"一次性 burner 地址"**，简单有效，未来可升级到环签名。

### 额外设计：burn 池（可选增强）

多个发送方将 MST 转入同一个"公共 burn 池"合约地址，从池中统一 burn。链上只能看到"池 burn 了 X MST"，无法区分具体是谁 burn 的。

```
A ──transfer──→ BurnPool
B ──transfer──→ BurnPool
C ──transfer──→ BurnPool
                  │
                  ▼
              burn 交易（Sender: BurnPool）
              链上只知道"池"burn了，不知道是谁
```

v1 暂不实现，作为未来隐私增强方向。

---

## 修复三：早期矿工激励（冷启动问题）

### 问题

纯手续费经济，早期交易量少，矿工收入接近零，无人愿挖矿，网络不安全。

### 方案：递减区块奖励（Halving Schedule）

前 N 年设置区块奖励，逐步递减至零，过渡到纯手续费经济。

```go
func BlockReward(height uint64) uint64 {
    // 前 4 年（约 210,240 个块 @ 10min/块）：每块 50 MST
    // 4-8 年：每块 25 MST
    // 8-12 年：每块 12.5 MST
    // 12-16 年：每块 6.25 MST
    // 16+ 年：0（纯手续费）

    halvings := height / 210_240  // 约每 4 年减半
    if halvings >= 4 {
        return 0
    }
    reward := uint64(50)
    for i := uint64(0); i < halvings; i++ {
        reward /= 2
    }
    return reward
}
```

### 代币经济总量

```
创世供给（认领）:  21,000,000 MST（固定）
区块奖励（挖矿）:  约 4,200,000 MST（前 16 年，逐步递减）
总上限:           25,200,000 MST

分配:
  认领（2 年窗口）: 21,000,000 / 1000 = 21,000 节点
  挖矿（16 年）:    约 4,200,000 MST 给矿工
  burn 消耗:        只减不增
```

### 区块奖励的来源

区块奖励 = 新铸造的 MST（增发）。

**和白皮书"无增发"原则的冲突**：
- 白皮书 §8.4："创世初始算力一次性产出全部MST总量，后续只销毁、不增发"
- 区块奖励 = 增发 = 违反白皮书

**调和方案**（二选一）：

**方案 A：接受有限增发**
- 总量上限 2520 万（含区块奖励），有硬顶
- 区块奖励是预期内的、可预测的、递减的
- 类似比特币：有增发但有上限，最终趋近纯手续费

**方案 B：创世预分配矿工奖励池**
- 创世时预留 420 万 MST 作为"矿工奖励池"
- 每块从奖励池中释放 50 MST 给矿工
- 池耗尽后变为纯手续费
- **不增发**，总量仍为 2100 万

```go
// 方案 B：预分配矿工池
const MinerRewardPool uint64 = 4_200_000

func BlockReward(height uint64, poolRemaining uint64) uint64 {
    if poolRemaining == 0 {
        return 0
    }
    halvings := height / 210_240
    if halvings >= 4 {
        return 0
    }
    base := uint64(50)
    for i := uint64(0); i < halvings; i++ {
        base /= 2
    }
    if base > poolRemaining {
        return poolRemaining
    }
    return base
}
```

**推荐方案 B**：不违反白皮书"无增发"原则，同时解决矿工激励问题。

### 矿工收入模型

```
第 1 年（高度 0 ~ 52,560）：
  区块奖励: 50 MST/块
  手续费:   ~1 MST/块（假设每天 100 笔交易，10 分钟/块）
  矿工总收入: 51 MST/块 ≈ 267 万 MST/年

第 5 年（高度 210,240 ~ 262,800）：
  区块奖励: 25 MST/块
  手续费:   ~5 MST/块（网络增长）
  矿工总收入: 30 MST/块 ≈ 157 万 MST/年

第 17 年+：
  区块奖励: 0
  手续费:   ~10 MST/块（假设成熟期）
  矿工总收入: 10 MST/块 ≈ 52 万 MST/年
```

---

## 修复后的完整交易类型

| Type | 说明 | 数据字段 | Fee | 备注 |
|------|------|----------|-----|------|
| `genesis_claim` | 创世认领 | 无 | 0 | 2 年窗口，1000 MST |
| `transfer` | MST 转账 | `To, Amount` | 1 MST | 支持 burner 地址 |
| `burn` | 发消息 burn | `MsgType, RefHash` | 按类型 | 一次性 burner 地址提交 |
| `register` | 节点注册 | `PubKey, XPub` | 1 MST | 首次上链 |
| `coinbase` | 区块奖励 | 无 | 0 | 矿工收入，从奖励池释放 |

---

## 修复后的完整数据包结构

```go
// DTN 数据包（协议层，现有结构 + BurnTicket）
type Packet struct {
    // 现有字段（不变）
    Version         int
    Kind            string
    PacketSHA1      string
    PacketSHA512    string
    TimestampUs     int64
    SenderID        string
    TargetID        string
    Broadcast       bool
    Alert           bool
    PoWAlgo         string
    PoWDifficulty   int
    PoWNonce        uint64
    PoWHash         string
    TTLMaxHops      int
    Hops            int
    SenderPubKey    string
    SenderX25519Pub string
    EphemeralX      string
    OPKUsed         string
    KeyBundle       json.RawMessage
    PayloadNonce    string
    PayloadCipher   string
    PayloadMAC      string
    ExpireAtUs      int64
    Signature       string

    // 新增：链上 burn 凭证
    BurnTicket    []byte `json:"burn_ticket,omitempty"`
    BurnTicketSig []byte `json:"burn_ticket_sig,omitempty"`
}
```

---

## 修复后的完整发送流程

```
A 要发消息给 B（DTN 模式）

1. 生成一次性 burner 密钥对 (burnerPub, burnerPriv)
2. 主账户 → burner 地址 transfer 11 MST（10 burn + 1 fee）
3. 用 burner 地址提交 burn 交易（MsgType: "dtn", RefHash: SHA256(密文)）
4. 向矿工请求 BurnTicket（矿工签名确认 burn 交易已在 mempool）
5. 构造 DTN 数据包：明文头部 + AES-GCM 加密载荷 + BurnTicket
6. 通过 UDP mesh 泛洪发送

B 收到消息：

1. 提取 BurnTicket
2. 验证矿工签名 ✅
3. 验证 RefHash == SHA256(密文) ✅
4. 验证 burn 金额 >= 10 MST ✅
5. 验证票据未过期 ✅
6. 验证 burn 交易在 mempool 或已上链 ✅
7. 全部通过 → 解密消息 → 展示
```

---

## 修复后的白皮书合规性

| 原则 | v3 状态 | 说明 |
|------|---------|------|
| 消息永不留存 | ✅ | 消息仅 DTN 内存，链上零内容 |
| 阅后即焚 | ✅ | DTN 层不变 |
| 消息零恢复 | ✅ | 链上无可解密内容 |
| 隐私绝对优先 | ✅ | burner 地址隐匿发送方 |
| 中继盲转发 | ✅ | DTN 层完整 |
| MST 仅约束发送 | ✅ | burn 是唯一消耗方式 |
| 无增发 | ✅ | 方案 B：预分配矿工池，不增发 |
| 去中心化 | ✅ | PoW + 矿工激励保障安全 |
| 抗封锁 | ✅ | DTN 层全部保留 |

---

## 开发路线更新

```
Phase 1（2 周）：最小可用链
  ├── 区块 + 交易数据结构（含 coinbase）
  ├── PoW 出块
  ├── genesis_claim + transfer + burn 交易
  ├── 矿工奖励池机制
  └── 单节点验证

Phase 2（2 周）：多节点共识
  ├── 区块广播
  ├── header-first 同步
  ├── 最长链 fork 解决
  └── 3 节点共识测试

Phase 3（1.5 周）：BurnTicket + 隐私
  ├── BurnTicket 签发/验证
  ├── 一次性 burner 地址
  ├── DTN 数据包附带 BurnTicket
  ├── 接收方验证逻辑
  └── 端到端测试

Phase 4（0.5 周）：打磨
  ├── CLI/GUI 扩展
  ├── 文档
  └── 压力测试
```

**总计约 6 周**。
